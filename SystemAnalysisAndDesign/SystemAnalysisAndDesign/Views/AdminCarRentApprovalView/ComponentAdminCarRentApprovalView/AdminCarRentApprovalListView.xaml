<UserControl x:Class="SystemAnalysisAndDesign.Views.AdminCarRentApprovalView.ComponentAdminCarRentApprovalView.AdminCarRentApprovalListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SystemAnalysisAndDesign.Views.AdminCarRentApprovalView.ComponentAdminCarRentApprovalView"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="984">

        <UserControl.Resources>
        <!-- Style cho Header cột -->
        <Style x:Key="CenteredHeader" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Height" Value="45"/>
        </Style>
        
        <!-- Style cho Cell -->
        <Style x:Key="CenteredCell" TargetType="DataGridCell">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
        
        <!-- Style cho Approve Button -->
        <Style x:Key="ApproveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <StackPanel Orientation="Horizontal" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Check" 
                                                       VerticalAlignment="Center" 
                                                       Margin="0,0,4,0"/>
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Style cho Reject Button -->
        <Style x:Key="RejectButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <StackPanel Orientation="Horizontal" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Close" 
                                                       VerticalAlignment="Center" 
                                                       Margin="0,0,4,0"/>
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E53935"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ~1~ Header Section @1@ -->
        <!-- <Border Grid.Row="0" Background="#F8F9FA" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="20"> -->
        <!--     <StackPanel> -->
        <!--         <TextBlock Text="Pending Car Rental Approvals" FontSize="24" FontWeight="Bold" Foreground="#333"/> -->
        <!--         <TextBlock Text="Review and approve or reject car rental requests from customers" FontSize="14" Foreground="#666"/> -->
        <!--     </StackPanel> -->
        <!-- </Border> -->

        <!-- Main Content -->
        <Grid Grid.Row="0" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <DataGrid Name="ApprovalDataGrid"
                      ItemsSource="{Binding PendingApprovals}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      RowHeight="120"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E0E0E0"
                      AlternatingRowBackground="#FAFBFC"
                      Background="White"
                      BorderThickness="1"
                      BorderBrush="#E0E0E0"
                      HeadersVisibility="Column"
                      Margin="0"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <!-- Car Model Column -->
                    <DataGridTemplateColumn Header="Car Model" Width="280">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="10">
                                    <Border Width="100" Height="70" CornerRadius="8" Background="#F5F5F5" Margin="0,0,15,0">
                                        <Image Source="{Binding Car.ImageUrl}" Stretch="UniformToFill"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock FontWeight="Bold" FontSize="14">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} {1}">
                                                    <Binding Path="Car.Brand"/>
                                                    <Binding Path="Car.Model"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                        <TextBlock Text="{Binding Car.CarType}" FontSize="12" Foreground="#666"/>
                                        <TextBlock Text="{Binding Car.LicensePlate}" FontSize="11" Foreground="#888"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!-- Customer Column -->
                    <DataGridTemplateColumn Header="Customer" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Margin="10" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Customer.FullName}" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{Binding Customer.Email}" FontSize="12" Foreground="#666"/>
                                    <TextBlock Text="{Binding Customer.PhoneNumber}" FontSize="12" Foreground="#666"/>
                                    <TextBlock Text="{Binding Customer.LicenseNumber}" FontSize="11" Foreground="#888"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!-- Pickup Date Column -->
                    <DataGridTextColumn Header="Pickup Date" Width="140" Binding="{Binding PickupDate, StringFormat='dd/MM/yyyy HH:mm'}"/>
                    <!-- Dropoff Date Column -->
                    <DataGridTextColumn Header="Dropoff Date" Width="140" Binding="{Binding DropoffDate, StringFormat='dd/MM/yyyy HH:mm'}"/>
                    <!-- Action Buttons Column -->
                    <DataGridTemplateColumn Header="Actions" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Approve"
                                            Style="{StaticResource ApproveButtonStyle}"
                                            Command="{Binding DataContext.ApproveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="Reject"
                                            Style="{StaticResource RejectButtonStyle}"
                                            Command="{Binding DataContext.RejectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
